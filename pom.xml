<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <parent>
		<artifactId>ingrid-base-pom</artifactId>
		<groupId>de.ingrid</groupId>
		<version>1.0.0</version>
	</parent>
  <modelVersion>4.0.0</modelVersion>
	<groupId>de.ingrid</groupId>
	<artifactId>ingrid-releases-pom</artifactId>
	<packaging>pom</packaging>
	<version>1.0.0-SNAPSHOT</version>
	<name>Ingrid Release POM</name>
	<scm>
		<connection>
			scm:svn:https://harrison.its-technidata.de:444/svn/ingrid/ingrid-releases-pom/trunk
		</connection>
		<developerConnection>
			scm:svn:https://harrison.its-technidata.de:444/svn/ingrid/ingrid-releases-pom/trunk
		</developerConnection>
	</scm>
  <dependencies>
    <dependency>
      <groupId>de.ingrid</groupId>
      <artifactId>ingrid-installer</artifactId>
    </dependency>
  </dependencies>
  <repositories>
		<repository>
			<id>de.ingrid.m2</id>
			<name>Maven2 Repo InGrid</name>
			<url>http://harrison.its-technidata.de/ingrid/maven2</url>
		</repository>
	</repositories>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>unpack</id>
            <phase>process-resources</phase>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>de.ingrid</groupId>
                  <artifactId>ingrid-installer</artifactId>
                  <type>jar</type>
                </artifactItem>
              </artifactItems>
              <!-- other configurations here -->
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>unpack</id>
						<phase>compile</phase>
						<configuration>
							<tasks>
								<unjar dest="target/installer">
									<fileset dir="target/dependency/lib">
										<include name="**/*.jar" />
									</fileset>
								</unjar>
								<copy todir="target/installer/resources">
									<fileset dir="target/dependency/resources" />
								</copy>
							</tasks>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-changes-plugin</artifactId>
        <configuration>
          <smtpHost>localhost</smtpHost>
          <subject>[ingrid update announcement] ${artifactId}-${version}</subject>
          <mailSender>
            <name>Release Notification</name>
            <email>noreply@wemove.com</email>
          </mailSender>
          <toAddresses>
            <toAddress implementation="java.lang.String">ingrid-announcement@wemove.com</toAddress>
          </toAddresses>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>wagon-maven-plugin</artifactId>
        <version>1.0-beta-1</version>
        <configuration>
          <fromDir>target</fromDir>
          <includes>${build.finalName}-installer.jar,CHANGELOG.TXT</includes>
          <url>file:///opt/ingrid/distributions/${project.artifactId}/${project.version}</url>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-release-plugin</artifactId>
        <version>2.0-beta-9</version>
        <configuration>
          <!--
            During release:perform, execute deploy changes:changes-report
            assembly:assembly wagon:upload changes:announcement-mail to upload
            the distribution to /opt/ingrid/distributions
          -->
          <goals>deploy changes:changes-report assembly:assembly wagon:upload changes:announcement-mail -Dproject.url=</goals>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <id>not_derived</id>
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-release-plugin</artifactId>
            <version>2.0-beta-9</version>
            <configuration>
              <goals>deploy</goals>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
